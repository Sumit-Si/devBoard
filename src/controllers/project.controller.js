const createProject = async (req,res) => {
    const {name,description} = req.body;
    const userId = req?.user?.id;

    try {
        // const user = await 
    } catch (error) {
        
    }
}

export {
    createProject
}